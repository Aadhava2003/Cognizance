Declare main function
Create a variable named num of type int
print "Enter the number"
Get the input as num
print the result from the function divcheck(num)

Function divcheck:
Declare function named divcheck of type int
Declare variables sum and rem of type int
Assign sum=0
While num>=1
If "True"
	rem=num%10
	num=num/10
	sum=sum+rem
If "False"
	Go to while sum>0
		If "True"
			Assign sum=sum-3
		If "False"
		    	Go to if sum==0
			      If "True"
				  print "TRUE"
			      else "FALSE"
				  print"FALSE"
				
				
	
	 